export function generateCSS() {
  let str = "";
  // MIND: currently len is not casted against total and will throw an error
  // if total is < len.
  for ( var i = 0; i < 6 ; i++ ){
    str += "\nimg";
  }

  return str;
}



